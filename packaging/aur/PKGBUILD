# Maintainer: JustRelaXX <https://github.com/JustRelaXX>
pkgname=keepr-bin
pkgver=0.1.4
pkgrel=1
pkgdesc="Local-first desktop companion for home care reminders"
arch=('x86_64')
url="https://github.com/JustRelaXX/Keepr"
license=('MIT')
depends=('cairo' 'desktop-file-utils' 'gdk-pixbuf2' 'glib2' 'gtk3' 'hicolor-icon-theme' 'libsoup' 'pango' 'webkit2gtk-4.1')
optdepends=('libappindicator-gtk3: system tray icon')
options=('!strip' '!debug')
install=keepr-bin.install
source_x86_64=("${pkgname}-${pkgver}.deb::${url}/releases/download/v${pkgver}/Keepr_${pkgver}_amd64.deb")
sha256sums_x86_64=('bf32de9d095213b2a32151f0cc0a95b4f8241812f09b093a37b073ea268d9cdf')
package() {
  tar -xf data.tar.* -C "${pkgdir}"
}
